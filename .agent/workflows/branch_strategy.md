---
description: AIによる自動開発ワークフロー - ブランチ戦略とPRフロー
---

# ブランチ戦略

## ブランチ構成

- **<main_branch>** - メインブランチ（保護、直接プッシュ禁止）
  - project.json の `main_branch` を参照（デフォルト: `main`）
- **<branch_prefix>/\*** - AI開発ブランチ
  - project.json の `branch_prefix` を参照（デフォルト: `ai`）

## 自動ワークフロー

Claude Codeに指示を出したら、以下のフローを自動実行する：

### 1. ブランチ作成

```bash
# 現在のブランチを確認
git status
git branch

# メインブランチから最新を取得
git checkout <main_branch>
git pull origin <main_branch> 2>/dev/null || true

# 作業ブランチを作成
git checkout -b <branch_prefix>/feature-name
```

**ブランチ命名規則：**
- 機能追加：`<prefix>/feature-<機能名>`
- バグ修正：`<prefix>/fix-<バグ内容>`
- リファクタリング：`<prefix>/refactor-<対象>`
- ドキュメント：`<prefix>/docs-<内容>`

### 2. 開発

機能実装、バグ修正、リファクタリングなどを実施。

### 3. テスト作成・実行

```bash
bash .agent/workflows/run_tests.sh
```

### 4. コードフォーマット & リント

```bash
bash .agent/workflows/check_and_format.sh
```

### 5. コミット

変更を論理的な単位に分割してコミット：
- 詳細：`.agent/workflows/git_commit.md` を参照

### 6. PR作成

```bash
# リモートブランチにプッシュ
git push -u origin <branch_prefix>/feature-name

# GitHub CLIでPR作成
gh pr create \
  --base <main_branch> \
  --head <branch_prefix>/feature-name \
  --title "feat: 機能名の追加" \
  --body "$(cat <<'EOF'
## 概要
[変更内容の簡潔な説明]

## 変更詳細
- [変更点1]
- [変更点2]

## テスト
- [x] 動作確認済み

🤖 Generated by [Claude Code](https://claude.com/claude-code)
EOF
)"
```

### 7. 人間によるレビュー

- PRをレビュー
- 必要に応じてフィードバック
- 承認後、メインにマージ

## ワークフロー実行時の注意

1. **必ずメインブランチから分岐する**
2. **1つのPRは1つの目的に集中** - 機能追加とバグ修正を混ぜない
3. **コミット前に必ずフォーマット・テスト実行**
4. **意味のあるコミットメッセージ** - git_commit.mdの規約に従う

## トラブルシューティング

### ブランチが既に存在する場合
```bash
git branch -D <branch_prefix>/feature-name
git checkout -b <branch_prefix>/feature-name
```

### メインブランチとの差分が大きい場合
```bash
git checkout <branch_prefix>/feature-name
git rebase <main_branch>
```
